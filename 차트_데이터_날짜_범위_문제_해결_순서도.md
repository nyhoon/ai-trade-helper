# 차트 데이터 날짜 범위 문제 해결 순서도

## 🔍 문제 분석

### 현재 상황
- **예상**: 20250926까지 데이터가 있어야 함
- **실제**: 20250924까지만 데이터 존재
- **차이**: 2일간의 데이터 누락

## 📊 문제 원인 분석

### 1. 증분 업데이트 로직 문제
```dart
// lib/core/data/incremental_data_manager.dart:192-196
final latestDateTime = DateFormat('yyyy-MM-dd').parse(latestDate);
final updateStartDate = DateFormat('yyyy-MM-dd').format(
  latestDateTime.subtract(const Duration(days: 4))
);
```

**문제점**: 최신 날짜로부터 4일 전까지만 업데이트하므로, 주말이나 휴일이 있으면 최신 데이터를 놓칠 수 있음

### 2. API 호출 날짜 범위 설정
```dart
// lib/core/api/kis_unified_api_service.dart:660-662
final now = DateTime.now();
final endDate = '${now.year}${now.month.toString().padLeft(2, '0')}${now.day.toString().padLeft(2, '0')}';
final startDate = now.subtract(Duration(days: count)).toString().replaceAll('-', '').substring(0, 8);
```

**문제점**: API 호출 시 오늘 날짜를 기준으로 하지만, 실제 거래일이 아닐 수 있음

### 3. 데이터 저장 로직 문제
```dart
// lib/core/database/repositories/historical_data_repository.dart:17-38
Future<void> upsertDailyBars({
  required String stockCode,
  required String market,
  required List<Map<String, dynamic>> bars,
  int keepDays = 100,
}) async {
  // Firestore 직접 쓰기 비활성화 - 서버 Functions를 통해서만 데이터 저장
  print('📊 $stockCode: ${bars.length}일 차트 데이터는 서버 Functions를 통해 저장됩니다');
  print('⚠️ 클라이언트에서 Firestore 직접 쓰기 비활성화됨 (권한 문제 방지)');
}
```

**문제점**: 클라이언트에서 Firestore 직접 쓰기가 비활성화되어 있어 데이터 저장이 제대로 되지 않을 수 있음

## 🔧 해결 방안

### 1. 증분 업데이트 로직 개선
```dart
// 개선된 증분 업데이트 로직
Future<void> _updateStockIncrementalData(String stockCode, String market) async {
  try {
    // 1. 최신 날짜 확인
    final latestDate = await _historicalDataRepo.getLatestDate(stockCode);
    if (latestDate == null) {
      await _loadStockInitialData(stockCode, market);
      return;
    }

    // 2. 최신 날짜로부터 7일 전까지 업데이트 (4일 → 7일로 확장)
    final latestDateTime = DateFormat('yyyy-MM-dd').parse(latestDate);
    final updateStartDate = DateFormat('yyyy-MM-dd').format(
      latestDateTime.subtract(const Duration(days: 7)) // 4 → 7로 변경
    );
    
    // 3. 오늘 날짜까지의 데이터 조회
    final today = DateFormat('yyyy-MM-dd').format(DateTime.now());
    
    // 4. API에서 최신 데이터 조회 (count를 늘려서 충분한 데이터 확보)
    final chartData = await _unifiedApiService!.getDomesticDailyChart(
      stockCode: stockCode, 
      count: 20 // 10 → 20으로 증가
    );
    
    // 5. 최신 데이터만 필터링하여 저장
    final newData = chartData.where((data) {
      final dataDate = data['date'] ?? '';
      return dataDate.compareTo(updateStartDate) >= 0;
    }).toList();
    
    // 6. 데이터 저장
    if (newData.isNotEmpty) {
      final bars = newData.map((e) => {
        'date': (e['date'] ?? '').toString().replaceAll('-', ''),
        'open': (e['open'] ?? 0.0).toDouble(),
        'high': (e['high'] ?? 0.0).toDouble(),
        'low': (e['low'] ?? 0.0).toDouble(),
        'close': (e['close'] ?? 0.0).toDouble(),
        'volume': (e['volume'] ?? 0).toInt(),
      }).toList();

      await _historicalDataRepo.upsertDailyBars(
        stockCode: stockCode,
        market: market,
        bars: bars,
        keepDays: 100,
      );
      print('📈 $stockCode: ${bars.length}일 증분 업데이트 완료');
    }
  } catch (e) {
    print('⚠️ $stockCode 증분 업데이트 실패: $e');
  }
}
```

### 2. API 호출 날짜 범위 개선
```dart
// 개선된 API 호출 날짜 설정
Future<List<Map<String, dynamic>>> getDomesticDailyChart({
  required String stockCode,
  String marketCode = 'J',
  String periodCode = 'D',
  String adjustedPrice = '1',
  int count = ChartConstants.CHART_MIN_BARS,
}) async {
  try {
    print('📈 [통일API] 국내주식 일별 차트 조회: $stockCode (기간: $periodCode, 개수: $count)');
    
    // 날짜 설정 개선 - 거래일 기준으로 조정
    final now = DateTime.now();
    
    // 주말인 경우 금요일로 조정
    DateTime endDate;
    if (now.weekday == DateTime.saturday) {
      endDate = now.subtract(const Duration(days: 1)); // 금요일
    } else if (now.weekday == DateTime.sunday) {
      endDate = now.subtract(const Duration(days: 2)); // 금요일
    } else {
      endDate = now;
    }
    
    final endDateStr = '${endDate.year}${endDate.month.toString().padLeft(2, '0')}${endDate.day.toString().padLeft(2, '0')}';
    final startDateStr = endDate.subtract(Duration(days: count + 10)).toString().replaceAll('-', '').substring(0, 8);
    
    // API 호출...
  } catch (e) {
    print('❌ 국내주식 일별 차트 조회 실패: $e');
    return [];
  }
}
```

### 3. 데이터 저장 로직 개선
```dart
// 개선된 데이터 저장 로직
Future<void> upsertDailyBars({
  required String stockCode,
  required String market,
  required List<Map<String, dynamic>> bars,
  int keepDays = 100,
}) async {
  if (bars.isEmpty) return;

  try {
    // 1. 서버 Functions를 통한 데이터 저장
    print('📊 $stockCode: ${bars.length}일 차트 데이터 서버 저장 요청');
    
    // 2. 로컬 SQLite에도 백업 저장
    await _saveToLocalDatabase(stockCode, bars);
    
    // 3. 서버 Functions 호출
    await _callServerFunctions(stockCode, bars);
    
    print('✅ $stockCode: ${bars.length}일 차트 데이터 저장 완료');
    
  } catch (e) {
    print('❌ $stockCode 차트 데이터 저장 실패: $e');
    // 저장 실패 시 재시도 로직 추가
    await _retrySaveData(stockCode, market, bars, keepDays);
  }
}
```

## 🚀 구현 순서

### 1단계: 증분 업데이트 로직 수정
- [ ] `_updateStockIncrementalData` 메서드 개선
- [ ] 업데이트 범위를 4일 → 7일로 확장
- [ ] API 호출 count를 10 → 20으로 증가

### 2단계: API 호출 날짜 범위 개선
- [ ] 주말/휴일 처리 로직 추가
- [ ] 거래일 기준 날짜 설정
- [ ] 충분한 데이터 확보를 위한 버퍼 추가

### 3단계: 데이터 저장 로직 강화
- [ ] 서버 Functions 호출 로직 개선
- [ ] 로컬 SQLite 백업 저장
- [ ] 저장 실패 시 재시도 로직

### 4단계: 테스트 및 검증
- [ ] 수동 데이터 업데이트 실행
- [ ] 최신 날짜 데이터 확인
- [ ] 증분 업데이트 정상 동작 확인

## 📋 예상 결과

### 수정 전
- 최신 데이터: 20250924
- 누락 데이터: 20250925, 20250926

### 수정 후
- 최신 데이터: 20250926 (또는 최신 거래일)
- 누락 데이터: 없음
- 안정적인 증분 업데이트

## 🔍 모니터링 포인트

1. **데이터 업데이트 빈도**: 매일 정기적으로 실행되는지 확인
2. **API 호출 성공률**: KIS API 호출 성공률 모니터링
3. **데이터 저장 성공률**: Firestore 저장 성공률 확인
4. **최신 날짜 확인**: 매일 최신 거래일 데이터가 정상적으로 저장되는지 확인

## ⚠️ 주의사항

1. **API 호출 제한**: KIS API 호출 제한을 고려하여 적절한 딜레이 설정
2. **서버 Functions 권한**: Firestore 쓰기 권한이 서버 Functions에 제대로 설정되어 있는지 확인
3. **데이터 중복 방지**: 동일한 날짜 데이터 중복 저장 방지
4. **에러 처리**: API 호출 실패 시 적절한 에러 처리 및 재시도 로직
