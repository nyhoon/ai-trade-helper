# 스플래시 데이터 최신화 해결 순서도

## 🔍 문제 상황
- ❌ "Bad state: No element" 오류 (국내주식 일별 차트 조회)
- ❌ 거래탭에서 NVD 거래량 0 표시
- ❌ 스플래시에서 차트데이터/현재가 최신화 부족

## 🛠️ 해결 방안

### 1. "Bad state: No element" 오류 해결
```
국내주식 일별 차트 조회
├── output2 데이터 확인
├── output.isNotEmpty 체크 추가 ✅
└── 빈 배열일 때 안전하게 처리
```

### 2. 스플래시 강제 데이터 최신화
```
스플래시 초기화
├── 1단계: 차트데이터 강제 최신화
│   ├── RemoteKisService.ensureChartAndAnalyze()
│   └── 서버에서 차트데이터 생성/업데이트
├── 2단계: 현재가 데이터 강제 최신화
│   ├── RemoteKisService.getCurrentPrice()
│   └── 서버에서 현재가 데이터 생성/업데이트
├── 3단계: 서버 실패 시 클라이언트 직접 호출
│   ├── 국내주식: KisUnifiedApiService.getDomesticStockPrice()
│   └── 해외주식: KisUnifiedApiService.getOverseasStockPrice()
└── 4단계: 거래량 데이터 강제 갱신
    ├── 해외주식만 거래량 강제 갱신
    ├── 서버에서 거래량 데이터 확인
    └── 클라이언트에서 직접 거래량 조회
```

### 3. 거래량 처리 로직
```
해외주식 거래량 처리
├── 1차: 현재 거래량 확인
│   ├── acml_vol (누적거래량)
│   ├── volume (거래량)
│   ├── bvol (매수거래량)
│   └── avol (매도거래량)
├── 2차: 현재 거래량이 0이면 전일 거래량 사용
│   ├── pvol (전일거래량)
│   └── prev_vol (이전거래량)
└── 3차: 여전히 0이면 정규장 시간 외로 판단
```

### 4. 백그라운드 처리
```
백그라운드 데이터 로딩
├── 필수 종목 (30개) 우선 처리
├── 나머지 종목 백그라운드 처리
├── 배치 크기: 3개씩 순차 처리
├── 메모리 정리: 200ms 대기
└── 거래량 갱신도 백그라운드에서 처리
```

## ✅ 예상 결과

### 1. "Bad state: No element" 오류
- ✅ **해결됨**: `output.isNotEmpty` 체크로 안전하게 처리

### 2. 거래량 0 문제
- ✅ **해결됨**: 현재 거래량이 0이면 전일 거래량 표시
- ✅ **해결됨**: 스플래시에서 강제 거래량 갱신

### 3. 데이터 최신화
- ✅ **해결됨**: 서버 우선 → 클라이언트 백업
- ✅ **해결됨**: 차트데이터 + 현재가 + 거래량 모두 강제 갱신

## 🔄 데이터 흐름

```
스플래시 시작
├── 관심종목/보유종목 목록 조회
├── 각 종목별 데이터 최신화
│   ├── 차트데이터 (서버 Functions)
│   ├── 현재가 (서버 Functions)
│   ├── 거래량 (해외주식만 강제 갱신)
│   └── 실패 시 클라이언트 직접 호출
├── 필수 종목 (30개) 우선 처리
├── 나머지 종목 백그라운드 처리
└── 메인 화면 진입
```

## 📊 로그 확인 포인트

### 성공 로그
- ✅ `$stockCode 차트데이터 강제 최신화 시도...`
- ✅ `$stockCode 현재가 데이터 강제 최신화 시도...`
- ✅ `$stockCode 데이터 최신화 성공: ${price['currentPrice']}`
- ✅ `$stockCode 거래량 갱신 성공: ${result['volume']}`

### 실패 로그
- ❌ `$stockCode 서버 실패 → 클라이언트 직접 호출...`
- ❌ `$stockCode 클라이언트 직접 호출 성공: ${clientPrice['currentPrice']}`
- ℹ️ `$stockCode 거래량 0은 정상 (정규장 시간 외)`

## 🎯 최종 목표

1. **"Bad state: No element" 오류 완전 해결**
2. **NVD 거래량 정상 표시** (전일 거래량이라도)
3. **스플래시에서 모든 데이터 강제 최신화**
4. **서버 실패 시 클라이언트 백업으로 데이터 보장**
