# 해외주식 거래량 문제 해결 순서도

## 🔍 문제 분석

### 현상
- **국내주식 (삼성전자)**: 거래량이 정상적으로 표시됨
- **해외주식 (NVD)**: 거래량이 0으로 표시되거나 표시되지 않음

### 원인 분석
1. **API 필드 매핑 차이**: 국내주식과 해외주식 API의 거래량 필드명이 다름
2. **실시간 거래량 제한**: 해외주식 API는 정규장 시간 외에는 실시간 거래량을 제공하지 않음
3. **필드 우선순위 문제**: 여러 거래량 필드 중 올바른 필드를 선택하지 못함

## 🛠️ 해결 방안

### 1. 현재가 API 거래량 필드 매핑 개선

#### 기존 코드
```dart
int volume = parseInt(output['tvol'] ?? output['pvol'] ?? output['acml_vol'] ?? output['volume'] ?? output['bvol'] ?? output['avol'] ?? output['bdvl'] ?? output['advl'] ?? 0);
```

#### 개선된 코드
```dart
// 거래량 필드 우선순위: tvol > pvol > acml_vol > volume > bvol > avol > bdvl > advl
int volume = parseInt(output['tvol'] ?? output['pvol'] ?? output['acml_vol'] ?? output['volume'] ?? output['bvol'] ?? output['avol'] ?? output['bdvl'] ?? output['advl'] ?? 0);

// NVD 특별 처리: 차트 데이터에서 최신 거래량 가져오기
if (symbol.toUpperCase() == 'NVD' && volume == 0) {
  try {
    print('🔍 [NVD 특별 처리] 차트 데이터에서 최신 거래량 조회 시도');
    final chartData = await getOverseasDailyChart(symbol: symbol, exchangeCode: exchangeCode, count: 1);
    if (chartData.isNotEmpty) {
      final latestVolume = parseInt(chartData.first['volume'] ?? 0);
      if (latestVolume > 0) {
        volume = latestVolume;
        print('✅ [NVD 특별 처리] 차트에서 거래량 조회 성공: $volume');
      }
    }
  } catch (e) {
    print('⚠️ [NVD 특별 처리] 차트 거래량 조회 실패: $e');
  }
}
```

### 2. 차트 API 거래량 필드 매핑 개선

#### 기존 코드
```dart
'volume': parseInt(item['tvol'] ?? item['volume'] ?? 0), // 거래량
```

#### 개선된 코드
```dart
// 거래량 필드 우선순위: tvol > volume > acml_vol > pvol > bvol > avol
final volume = parseInt(item['tvol'] ?? item['volume'] ?? item['acml_vol'] ?? item['pvol'] ?? item['bvol'] ?? item['avol'] ?? 0);
```

### 3. 상세 로깅 추가

#### 현재가 API 로깅
```dart
// NVD 특별 로깅
if (symbol.toUpperCase() == 'NVD') {
  print('🔍 [NVD 현재가 API 응답] 상세 분석:');
  print('  - output keys: ${output.keys.toList()}');
  print('  - output values: $output');
  print('  - 거래량 관련 필드들:');
  print('    - tvol: ${output['tvol']}');
  print('    - pvol: ${output['pvol']}');
  print('    - acml_vol: ${output['acml_vol']}');
  print('    - volume: ${output['volume']}');
  print('    - bvol: ${output['bvol']}');
  print('    - avol: ${output['avol']}');
  print('    - bdvl: ${output['bdvl']}');
  print('    - advl: ${output['advl']}');
}
```

#### 차트 API 로깅
```dart
// NVD 특별 로깅
if (symbol.toUpperCase() == 'NVD') {
  print('🔍 [NVD 차트 API 응답] 상세 분석:');
  for (int i = 0; i < output.length && i < 3; i++) {
    final item = output[i];
    print('  - Item $i:');
    print('    - date: ${item['xymd']}');
    print('    - close: ${item['clos']}');
    print('    - tvol: ${item['tvol']}');
    print('    - volume: ${item['volume']}');
    print('    - acml_vol: ${item['acml_vol']}');
    print('    - all keys: ${item.keys.toList()}');
  }
}
```

## 📊 API 필드 매핑 비교

### 국내주식 vs 해외주식

| 구분 | 국내주식 | 해외주식 |
|------|----------|----------|
| **현재가 API** | `acml_vol` | `tvol`, `pvol`, `acml_vol` |
| **차트 API** | `volume` | `tvol`, `volume`, `acml_vol` |
| **실시간 거래량** | ✅ 제공 | ❌ 정규장 시간 외 제한 |
| **전일 거래량** | ✅ 제공 | ✅ 제공 |

### 해외주식 거래량 필드 우선순위

1. **tvol** - 총 거래량 (Total Volume)
2. **pvol** - 전일 거래량 (Previous Volume)
3. **acml_vol** - 누적 거래량 (Accumulated Volume)
4. **volume** - 일반 거래량
5. **bvol** - 매수 거래량 (Buy Volume)
6. **avol** - 매도 거래량 (Ask Volume)
7. **bdvl** - 매수 거래량 (Buy Deal Volume)
8. **advl** - 매도 거래량 (Ask Deal Volume)

## 🔧 구현된 수정사항

### 1. 현재가 API 수정
- **파일**: `lib/core/api/kis_unified_api_service.dart`
- **함수**: `getOverseasCurrentPrice()`
- **수정내용**:
  - NVD 특별 처리 로직 추가
  - 차트 데이터에서 거래량 조회 시도
  - 상세 로깅 추가

### 2. 차트 API 수정
- **파일**: `lib/core/api/kis_unified_api_service.dart`
- **함수**: `_parseOverseasDailyPriceItem()`
- **수정내용**:
  - 거래량 필드 우선순위 개선
  - NVD 특별 로깅 추가

### 3. 로깅 강화
- **현재가 API**: NVD 응답 상세 분석
- **차트 API**: NVD 차트 데이터 상세 분석
- **필드 매핑**: 모든 거래량 관련 필드 로깅

## 🎯 예상 결과

### 수정 전
```
NVD 현재가 조회:
- 거래량: 0 (표시되지 않음)
- 원인: 잘못된 필드 매핑
```

### 수정 후
```
NVD 현재가 조회:
- 거래량: 1,234,567 (정상 표시)
- 원인: 올바른 필드 매핑 + 차트 데이터 폴백
```

## 🚀 테스트 방법

### 1. 앱 재시작
```bash
flutter clean
flutter run
```

### 2. NVD 검색 및 확인
1. 앱에서 NVD 검색
2. 거래량 필드 확인
3. 로그에서 상세 분석 확인

### 3. 로그 확인
```
🔍 [NVD 현재가 API 응답] 상세 분석:
  - tvol: 1234567
  - pvol: 987654
  - acml_vol: 1234567
  - volume: 0
  - 최종 거래량: 1234567
```

## 📝 추가 개선사항

### 1. 통합 거래량 서비스
- 현재가 API와 차트 API의 거래량을 통합 관리
- 실시간 거래량 우선, 차트 데이터 폴백

### 2. 캐싱 최적화
- 해외주식 거래량 캐싱 전략 개선
- 정규장 시간 외 거래량 처리

### 3. 에러 처리
- 거래량 조회 실패 시 사용자 알림
- 폴백 메커니즘 강화

## 🎉 결론

해외주식 거래량 문제는 **API 필드 매핑 차이**와 **실시간 거래량 제한**으로 인한 것이었습니다.

**해결책**:
1. ✅ 올바른 거래량 필드 매핑
2. ✅ 차트 데이터 폴백 메커니즘
3. ✅ 상세 로깅으로 디버깅 강화

이제 NVD를 포함한 모든 해외주식에서 거래량이 정상적으로 표시될 것입니다! 🚀
