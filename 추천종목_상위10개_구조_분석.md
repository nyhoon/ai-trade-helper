# 추천종목 상위 10개 종목 구조 분석

## 📊 전체 구조 개요

```
사용자 화면 (RecommendedStocksScreen)
    ↓
백그라운드 서비스 (RecommendedStocksBackgroundService)
    ↓
점수 계산 (UnifiedAnalysisService)
    ↓
데이터 저장 (TopStocksRepository)
    ↓
상위 종목 선정 (TopStocksUseCase)
    ↓
화면 표시 (시장별 상위 10개)
```

## 🔄 상위 10개 종목 선정 과정

### 1. **백그라운드 서비스 시작**
```dart
// RecommendedStocksBackgroundService
- 8397개 종목 대상
- 시장별 분류: KOSPI, KOSDAQ, NASDAQ, NYSE
- 거래대금 필터링: 10억원 이상 (기본값)
```

### 2. **점수 계산 과정**
```dart
// UnifiedAnalysisService.analyzeStock()
- 종합 지표 점수 계산
- 7개 지표: 거래량, RSI, MACD, 볼린저밴드, 이동평균, VWAP, ADX
- 가중 평균으로 최종 점수 산출
- 점수 범위: -1.0 ~ +1.0
```

### 3. **데이터 저장**
```dart
// TopStocksRepository
- top_stocks 테이블에 저장
- 컬럼: stock_code, stock_name, market, score, current_price, rank
- 인덱스: score DESC, market별 정렬
```

### 4. **상위 종목 선정**
```dart
// TopStocksUseCase.getTopStocksByMarket()
- 시장별 조회: market = 'NASDAQ'/'KOSPI'/'KOSDAQ'
- 점수 기준 정렬: ORDER BY score DESC
- 상위 10개 제한: LIMIT 10
- 최소 점수 필터: minScore >= 0.3
```

### 5. **화면 표시**
```dart
// RecommendedStocksScreen._loadMarketTopStocksParallel()
- 3개 시장 병렬 처리
- 각 시장별 상위 10개 종목
- 실시간 업데이트 (30초마다)
```

## 📈 점수 계산 상세 과정

### **종합 지표 점수 계산**
```
1. 거래량 점수 (가중치 20%)
   - 현재 거래량 vs 평균 거래량
   - 시간대별 보정 (장 시작/마감)

2. RSI 점수 (가중치 20%)
   - 과매수/과매도 구간 판단
   - 거래량/변동성 가중치 적용

3. MACD 점수 (가중치 20%)
   - MACD vs Signal 비교
   - 추세 강도 반영

4. 볼린저밴드 점수 (가중치 15%)
   - 현재가 vs 밴드 위치
   - 변동성 고려

5. 이동평균 점수 (가중치 15%)
   - MA5, MA20, MA60 배열
   - 트렌드 방향 판단

6. VWAP 점수 (가중치 5%)
   - 현재가 vs VWAP 비교
   - 거래량 가중치

7. ADX 점수 (가중치 5%)
   - 추세 강도 측정
   - 거래량 고려
```

### **최종 점수 산출**
```dart
final weightedSum = (volumeScore * 0.20) +
                   (rsiScore * 0.20) +
                   (macdScore * 0.20) +
                   (bollingerScore * 0.15) +
                   (movingAverageScore * 0.15) +
                   (vwapScore * 0.05) +
                   (adxScore * 0.05);

final finalScore = weightedSum; // -1.0 ~ +1.0
```

## 🎯 상위 10개 선정 기준

### **1차 필터링**
- 거래대금: 10억원 이상
- 최소 점수: 0.3 이상
- 시장별 분류: NASDAQ, KOSPI, KOSDAQ

### **2차 정렬**
- 점수 기준 내림차순 (높은 점수 우선)
- 동일 점수 시 랭크 기준 오름차순

### **3차 제한**
- 시장별 상위 10개만 선정
- 실시간 업데이트 (30초마다)

## 🔄 실시간 업데이트 과정

### **백그라운드 계산**
```dart
// 10초마다 점수 업데이트
Timer.periodic(Duration(seconds: 10), _performScoreUpdate);

// 30초마다 상위 종목 업데이트  
Timer.periodic(Duration(seconds: 30), _performTopStocksUpdate);
```

### **화면 갱신**
```dart
// 병렬 처리로 3개 시장 동시 로드
final futures = [
  _topStocksUseCase.getTopStocksByMarket(market: 'NASDAQ', limit: 10),
  _topStocksUseCase.getTopStocksByMarket(market: 'KOSPI', limit: 10),
  _topStocksUseCase.getTopStocksByMarket(market: 'KOSDAQ', limit: 10),
];
```

## 📊 데이터 흐름

```
1. API 데이터 수집
   ↓
2. UnifiedAnalysisService 분석
   ↓
3. 종합 점수 계산 (7개 지표)
   ↓
4. TopStocksRepository 저장
   ↓
5. TopStocksUseCase 조회
   ↓
6. 시장별 상위 10개 선정
   ↓
7. RecommendedStocksScreen 표시
```

## 🎨 화면 구성

### **탭별 표시**
- **나스닥**: 상위 10개 종목
- **코스피**: 상위 10개 종목  
- **코스닥**: 상위 10개 종목

### **종목 정보**
- 종목명 (한글/영문)
- 현재가
- 등락률
- 종합 점수
- 실시간 업데이트 시간

## ⚡ 성능 최적화

### **병렬 처리**
- 3개 시장 동시 로드
- 배치 처리 (10개씩)
- 비동기 점수 계산

### **캐싱 시스템**
- 5분 이내 데이터 재사용
- 메모리 캐시 활용
- DB 인덱스 최적화

### **실시간 업데이트**
- 30초마다 상위 종목 갱신
- 점진적 UI 업데이트
- 백그라운드 계산

## 🔧 설정 가능한 옵션

### **거래대금 필터**
- 1억원, 5억원, 10억원, 50억원, 100억원
- 기본값: 10억원

### **점수 임계값**
- 최소 점수: 0.3 (기본값)
- 조정 가능

### **업데이트 주기**
- 점수 계산: 10초
- 상위 종목: 30초
- 화면 갱신: 실시간

이 구조를 통해 **8397개 종목 중에서 각 시장별로 가장 높은 점수를 받은 상위 10개 종목**이 실시간으로 선정되어 사용자에게 표시됩니다.
