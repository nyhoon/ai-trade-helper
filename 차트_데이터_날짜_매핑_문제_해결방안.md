# 차트 데이터 날짜 매핑 문제 해결방안

## 🔍 문제 상황

### 실제 데이터 vs Firestore 데이터
**실제 데이터:**
- 2025.09.26: 23,038,221 (거래량)
- 2025.09.25: 19,665,151 (거래량) 
- 2025.09.24: 18,300,997 (거래량)

**Firestore 데이터:**
- 20250924: volume 18,300,997 ✅
- 20250926: volume 19,665,151 ❌ (25일 데이터가 26일로 잘못 저장됨)
- 20250927: volume 19,665,151 ❌ (중복)
- 20250928: volume 19,665,151 ❌ (중복)

## 🔍 문제 원인 분석

### 1. KIS API 응답 데이터 형식 문제
KIS API에서 받아온 데이터의 날짜 형식이 예상과 다를 수 있습니다.

### 2. 날짜 변환 로직 문제
```dart
// 현재 코드 (문제 가능성)
'date': (e['date'] ?? '').toString().replaceAll('-', ''),
```

### 3. 서버 Functions 저장 로직 문제
서버 Functions에서 차트 데이터를 저장할 때 날짜 매핑이 잘못될 수 있습니다.

## 🔧 해결방안

### 1. KIS API 응답 데이터 확인
먼저 KIS API에서 실제로 어떤 데이터가 오는지 확인해야 합니다.

```dart
// API 응답 데이터 로깅 추가
print('🔍 KIS API 응답 데이터: $chartData');
for (final item in chartData) {
  print('  - 날짜: ${item['date']}, 거래량: ${item['volume']}');
}
```

### 2. 날짜 변환 로직 개선
```dart
// 개선된 날짜 변환 로직
String _formatDateForStorage(dynamic dateValue) {
  if (dateValue == null) return '';
  
  String dateStr = dateValue.toString();
  
  // 이미 yyyyMMdd 형식인 경우
  if (dateStr.length == 8 && RegExp(r'^\d{8}$').hasMatch(dateStr)) {
    return dateStr;
  }
  
  // yyyy-MM-dd 형식인 경우
  if (dateStr.contains('-')) {
    return dateStr.replaceAll('-', '');
  }
  
  // 다른 형식인 경우 DateTime으로 파싱 시도
  try {
    final date = DateTime.parse(dateStr);
    return '${date.year}${date.month.toString().padLeft(2, '0')}${date.day.toString().padLeft(2, '0')}';
  } catch (e) {
    print('❌ 날짜 파싱 실패: $dateStr - $e');
    return '';
  }
}
```

### 3. 데이터 저장 전 검증 로직 추가
```dart
// 데이터 저장 전 검증
Future<void> _validateAndSaveChartData(String stockCode, List<Map<String, dynamic>> chartData) async {
  try {
    print('🔍 차트 데이터 검증 시작: $stockCode');
    
    for (final item in chartData) {
      final date = item['date']?.toString() ?? '';
      final volume = item['volume'] ?? 0;
      
      print('  - 원본 데이터: 날짜=$date, 거래량=$volume');
      
      // 날짜 형식 검증
      if (date.isEmpty) {
        print('❌ 날짜가 비어있음: $item');
        continue;
      }
      
      // 거래량 검증
      if (volume <= 0) {
        print('⚠️ 거래량이 0 이하: 날짜=$date, 거래량=$volume');
      }
    }
    
    // 검증된 데이터 저장
    await _saveValidatedChartData(stockCode, chartData);
    
  } catch (e) {
    print('❌ 차트 데이터 검증 실패: $e');
  }
}
```

### 4. 중복 데이터 방지 로직
```dart
// 중복 데이터 방지
Future<void> _saveValidatedChartData(String stockCode, List<Map<String, dynamic>> chartData) async {
  try {
    final db = FirebaseFirestore.instance;
    final batch = db.batch();
    
    for (final item in chartData) {
      final date = _formatDateForStorage(item['date']);
      if (date.isEmpty) continue;
      
      // 기존 데이터 확인
      final docRef = db.collection('charts').doc(stockCode).collection('daily').doc(date);
      final existingDoc = await docRef.get();
      
      if (existingDoc.exists) {
        final existingData = existingDoc.data()!;
        final existingVolume = existingData['volume'] ?? 0;
        final newVolume = item['volume'] ?? 0;
        
        // 거래량이 다르면 업데이트
        if (existingVolume != newVolume) {
          print('🔄 데이터 업데이트: $date (거래량: $existingVolume → $newVolume)');
          batch.set(docRef, {
            'date': date,
            'date_ts': int.parse(date),
            'open': item['open'] ?? 0,
            'high': item['high'] ?? 0,
            'low': item['low'] ?? 0,
            'close': item['close'] ?? 0,
            'volume': item['volume'] ?? 0,
            'stock_code': stockCode,
            'market': 'KOSPI',
            'updated_at': DateTime.now().millisecondsSinceEpoch,
          }, SetOptions(merge: true));
        } else {
          print('⏭️ 데이터 동일: $date (거래량: $existingVolume)');
        }
      } else {
        print('➕ 새 데이터 추가: $date (거래량: ${item['volume']})');
        batch.set(docRef, {
          'date': date,
          'date_ts': int.parse(date),
          'open': item['open'] ?? 0,
          'high': item['high'] ?? 0,
          'low': item['low'] ?? 0,
          'close': item['close'] ?? 0,
          'volume': item['volume'] ?? 0,
          'stock_code': stockCode,
          'market': 'KOSPI',
          'updated_at': DateTime.now().millisecondsSinceEpoch,
        });
      }
    }
    
    await batch.commit();
    print('✅ 차트 데이터 저장 완료: $stockCode');
    
  } catch (e) {
    print('❌ 차트 데이터 저장 실패: $e');
  }
}
```

## 🚀 구현 순서

### 1단계: API 응답 데이터 확인
- [ ] KIS API 응답 데이터 로깅 추가
- [ ] 실제 데이터 형식 확인
- [ ] 날짜 형식 분석

### 2단계: 날짜 변환 로직 개선
- [ ] `_formatDateForStorage()` 함수 구현
- [ ] 다양한 날짜 형식 지원
- [ ] 에러 처리 강화

### 3단계: 데이터 검증 로직 추가
- [ ] `_validateAndSaveChartData()` 함수 구현
- [ ] 데이터 무결성 검증
- [ ] 로깅 강화

### 4단계: 중복 데이터 방지
- [ ] 기존 데이터 확인 로직
- [ ] 배치 업데이트 구현
- [ ] 중복 방지 메커니즘

### 5단계: 테스트 및 검증
- [ ] 삼성전자 데이터 수동 업데이트
- [ ] 날짜 매핑 정확성 확인
- [ ] 중복 데이터 제거 확인

## 📊 예상 결과

### 수정 전
- 20250924: 18,300,997 ✅
- 20250926: 19,665,151 ❌ (25일 데이터)
- 20250927: 19,665,151 ❌ (중복)
- 20250928: 19,665,151 ❌ (중복)

### 수정 후
- 20250924: 18,300,997 ✅
- 20250925: 19,665,151 ✅ (25일 데이터)
- 20250926: 23,038,221 ✅ (26일 데이터)
- 중복 데이터 없음 ✅

## 🔍 모니터링 포인트

1. **API 응답 데이터**: KIS API에서 실제로 어떤 데이터가 오는지 확인
2. **날짜 변환**: 날짜 형식 변환이 올바르게 되는지 확인
3. **데이터 저장**: Firestore에 올바른 날짜로 저장되는지 확인
4. **중복 방지**: 중복 데이터가 생성되지 않는지 확인

## ⚠️ 주의사항

1. **데이터 무결성**: 기존 데이터 손실 방지
2. **성능 최적화**: 배치 업데이트로 성능 개선
3. **에러 처리**: 날짜 파싱 실패 시 적절한 처리
4. **로깅**: 상세한 로깅으로 디버깅 용이성 확보
