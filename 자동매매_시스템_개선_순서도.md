# 🔄 자동매매 시스템 개선 완료 순서도

## 📊 **전체 시스템 아키텍처**

```mermaid
graph TB
    subgraph "📱 사용자 인터페이스"
        UI[메인 화면]
        SETTINGS[설정 화면]
        ALERTS[알림 탭]
        HISTORY[내역 탭]
    end
    
    subgraph "🔄 백그라운드 서비스"
        BGS[BackgroundTradingService]
        TIMER[3분 타이머 + 지터]
        LOCK[재진입 방지 락]
    end
    
    subgraph "🎯 UseCase 계층"
        SYNC_ACCOUNT[SyncAccountInfoUseCase]
        SYNC_UNIVERSE[SyncUniverseUseCase]
        ANALYZE[AnalyzeSignalsUseCase]
        EXECUTE[ExecuteOrdersUseCase]
        NOTIFY[PersistNotificationUseCase]
        TRACK[TrackOrderStatusUseCase]
    end
    
    subgraph "🏦 API 계층"
        KIS_API[KisApiService]
        ACCOUNT_API[KisAccountApi]
        ORDERS_API[KisOrdersApi]
        MARKET_API[KisMarketDataApi]
        CHARTS_API[KisChartsApi]
    end
    
    subgraph "🗄️ 데이터 계층"
        DB[(SQLite DB)]
        SIGNALS[signal_history]
        NOTIFICATIONS[notification_history]
        TRADES[trade_history]
        HOLDINGS[holdings]
        WATCHLIST[watchlist]
        ACCOUNT[account_info]
    end
    
    subgraph "⚙️ 설정 관리"
        STYLE_MGR[InvestmentStyleManager]
        STYLES[투자 스타일 설정]
    end
    
    UI --> BGS
    SETTINGS --> STYLE_MGR
    BGS --> TIMER
    TIMER --> LOCK
    LOCK --> SYNC_ACCOUNT
    SYNC_ACCOUNT --> ACCOUNT_API
    ACCOUNT_API --> KIS_API
    KIS_API --> DB
    
    SYNC_UNIVERSE --> ACCOUNT_API
    ANALYZE --> MARKET_API
    EXECUTE --> ORDERS_API
    NOTIFY --> DB
    TRACK --> ORDERS_API
    
    STYLE_MGR --> STYLES
    STYLES --> ANALYZE
    STYLES --> EXECUTE
    
    DB --> UI
    DB --> ALERTS
    DB --> HISTORY
```

## 🔄 **3분 주기 자동매매 사이클**

```mermaid
flowchart TD
    START([자동매매 AI ON]) --> TIMER[3분 타이머 시작]
    TIMER --> JITTER[지터 ±15초 적용]
    JITTER --> CHECK_LOCK{재진입 락 확인}
    CHECK_LOCK -->|락 있음| SKIP[이전 사이클 건너뜀]
    CHECK_LOCK -->|락 없음| SET_LOCK[락 설정]
    
    SET_LOCK --> NETWORK{네트워크 연결 확인}
    NETWORK -->|연결 없음| WAIT_NET[네트워크 대기]
    NETWORK -->|연결됨| SYNC_ACCOUNT[1. 계좌 정보 동기화]
    
    SYNC_ACCOUNT --> LOAD_STYLE[투자 스타일 설정 로드]
    LOAD_STYLE --> SYNC_UNIVERSE[2. 관심종목/보유종목 동기화]
    SYNC_UNIVERSE --> ANALYZE[3. 시그널 분석]
    
    ANALYZE --> RSI_CHECK{RSI 조건 검증}
    RSI_CHECK -->|불충족| NEXT_STOCK[다음 종목]
    RSI_CHECK -->|충족| DUP_CHECK{중복 시그널 확인}
    
    DUP_CHECK -->|중복| SKIP_SIGNAL[시그널 건너뜀]
    DUP_CHECK -->|새로운| SAVE_SIGNAL[시그널 DB 저장]
    
    SAVE_SIGNAL --> HAS_SIGNALS{시그널 존재?}
    HAS_SIGNALS -->|없음| TRACK_ORDERS[6. 주문 상태 추적]
    HAS_SIGNALS -->|있음| EXECUTE_ORDERS[4. 주문 실행]
    
    EXECUTE_ORDERS --> POSITION_SIZE[투자 스타일 기반 포지션 사이징]
    POSITION_SIZE --> MAX_STOCKS{최대 종목 수 확인}
    MAX_STOCKS -->|초과| SKIP_BUY[매수 건너뜀]
    MAX_STOCKS -->|여유| CALC_AMOUNT[주문 금액 계산]
    
    CALC_AMOUNT --> MIN_QTY{최소 주문 수량 확인}
    MIN_QTY -->|부족| SKIP_ORDER[주문 건너뜀]
    MIN_QTY -->|충족| SEND_ORDER[주문 전송]
    
    SEND_ORDER --> SAVE_TRADE[거래 내역 DB 저장]
    SAVE_TRADE --> NOTIFY[5. 알림 처리]
    
    NOTIFY --> DUP_NOTIFY{중복 알림 확인}
    DUP_NOTIFY -->|중복| SKIP_NOTIFY[알림 건너뜀]
    DUP_NOTIFY -->|새로운| SAVE_NOTIFY[알림 DB 저장]
    SAVE_NOTIFY --> PUSH[푸시 알림 발송]
    
    PUSH --> TRACK_ORDERS
    TRACK_ORDERS --> CHECK_STATUS{주문 상태 확인}
    CHECK_STATUS -->|체결| UPDATE_TRADE[거래 내역 업데이트]
    CHECK_STATUS -->|대기| NEXT_CYCLE[다음 사이클]
    
    UPDATE_TRADE --> ERROR_COUNT{오류 개수 확인}
    ERROR_COUNT -->|0-2개| NORMAL_WAIT[정상 대기 3분]
    ERROR_COUNT -->|3개 이상| LONG_WAIT[5분 대기]
    ERROR_COUNT -->|전체 실패| FAIL_WAIT[2분 대기]
    
    NORMAL_WAIT --> RELEASE_LOCK[락 해제]
    LONG_WAIT --> RELEASE_LOCK
    FAIL_WAIT --> RELEASE_LOCK
    RELEASE_LOCK --> NEXT_CYCLE
    NEXT_CYCLE --> TIMER
```

## 💰 **투자 스타일별 포지션 사이징**

```mermaid
flowchart LR
    subgraph "🎯 투자 스타일 선택"
        CONSERVATIVE[보수적]
        MODERATE[일반적]
        AGGRESSIVE[공격적]
    end
    
    subgraph "📊 포지션 사이징"
        CONSERVATIVE --> CONS_SIZE[5% 포지션]
        MODERATE --> MOD_SIZE[10% 포지션]
        AGGRESSIVE --> AGG_SIZE[15% 포지션]
    end
    
    subgraph "🔢 최대 종목 수"
        CONS_SIZE --> CONS_MAX[3개 종목]
        MOD_SIZE --> MOD_MAX[5개 종목]
        AGG_SIZE --> AGG_MAX[7개 종목]
    end
    
    subgraph "📈 RSI 조건"
        CONS_MAX --> CONS_RSI[RSI ≤ 35]
        MOD_MAX --> MOD_RSI[RSI ≤ 30]
        AGG_MAX --> AGG_RSI[RSI ≤ 45]
    end
    
    CONS_RSI --> BUY_LOGIC[매수 로직]
    MOD_RSI --> BUY_LOGIC
    AGG_RSI --> BUY_LOGIC
    
    BUY_LOGIC --> CALC[주문 금액 = 주문가능금액 × 포지션사이즈]
    CALC --> QTY[주문 수량 = 주문금액 ÷ 현재가격]
    QTY --> MIN_CHECK{최소 1주 확인}
    MIN_CHECK -->|충족| EXECUTE[주문 실행]
    MIN_CHECK -->|부족| SKIP[주문 건너뜀]
```

## 🔍 **시그널 분석 및 중복 방지**

```mermaid
flowchart TD
    UNIVERSE[관심종목 + 보유종목] --> STOCK[종목별 분석]
    STOCK --> PRICE[현재가 조회]
    PRICE --> ANALYSIS[AI 분석 실행]
    ANALYSIS --> SIGNAL{시그널 생성?}
    
    SIGNAL -->|없음| NEXT[다음 종목]
    SIGNAL -->|있음| RSI_VALID{RSI 조건 검증}
    
    RSI_VALID -->|불충족| NEXT
    RSI_VALID -->|충족| DEDUP_KEY[dedupKey 생성]
    
    DEDUP_KEY --> DUP_CHECK{1시간 내 중복 확인}
    DUP_CHECK -->|중복| SKIP_SIGNAL[시그널 건너뜀]
    DUP_CHECK -->|새로운| SAVE[시그널 DB 저장]
    
    SAVE --> UNIQUE_CHECK{UNIQUE 제약 확인}
    UNIQUE_CHECK -->|충돌| SKIP_SIGNAL
    UNIQUE_CHECK -->|성공| ADD_RESULT[결과 리스트 추가]
    
    ADD_RESULT --> MORE{더 분석할 종목?}
    MORE -->|있음| STOCK
    MORE -->|없음| RETURN[시그널 리스트 반환]
```

## 📱 **알림 시스템 개선**

```mermaid
flowchart TD
    SIGNALS[시그널 리스트] --> SIGNAL[시그널별 처리]
    SIGNAL --> MESSAGE[상세 메시지 생성]
    
    MESSAGE --> DETAILS[RSI, 신뢰도 포함]
    DETAILS --> STYLE_NAME[투자 스타일명 추가]
    STYLE_NAME --> DUP_CHECK{30분 내 중복 확인}
    
    DUP_CHECK -->|중복| SKIP[알림 건너뜀]
    DUP_CHECK -->|새로운| SAVE_DB[알림 DB 저장]
    
    SAVE_DB --> PUSH_TYPE{시그널 타입}
    PUSH_TYPE -->|매수| BUY_NOTIFY[매수 알림 발송]
    PUSH_TYPE -->|매도| SELL_NOTIFY[매도 알림 발송]
    PUSH_TYPE -->|정보| INFO_NOTIFY[정보 알림 발송]
    
    BUY_NOTIFY --> STYLE_TITLE[투자 스타일 제목]
    SELL_NOTIFY --> STYLE_TITLE
    INFO_NOTIFY --> STYLE_TITLE
    
    STYLE_TITLE --> FINAL[최종 푸시 알림]
    FINAL --> MORE_SIGNALS{더 처리할 시그널?}
    MORE_SIGNALS -->|있음| SIGNAL
    MORE_SIGNALS -->|없음| COMPLETE[알림 처리 완료]
```

## 🔄 **주문 상태 추적**

```mermaid
flowchart TD
    START[주문 상태 추적 시작] --> PENDING[대기 주문 조회]
    PENDING --> ORDER[주문별 처리]
    
    ORDER --> STATUS{주문 상태 확인}
    STATUS -->|체결 완료| EXECUTED[체결 완료 처리]
    STATUS -->|부분 체결| PARTIAL[부분 체결 처리]
    STATUS -->|대기 중| WAITING[대기 상태 로그]
    STATUS -->|취소/실패| FAILED[실패 상태 로그]
    
    EXECUTED --> UPDATE_TRADE[거래 내역 업데이트]
    PARTIAL --> UPDATE_PARTIAL[부분 체결 기록]
    WAITING --> LOG_WAIT[대기 상태 기록]
    FAILED --> LOG_FAIL[실패 상태 기록]
    
    UPDATE_TRADE --> STYLE_REASON[투자 스타일 사유 추가]
    UPDATE_PARTIAL --> STYLE_REASON
    STYLE_REASON --> MORE_ORDERS{더 처리할 주문?}
    
    MORE_ORDERS -->|있음| ORDER
    MORE_ORDERS -->|없음| COMPLETE[추적 완료]
```

## ⚡ **에러 처리 및 복구**

```mermaid
flowchart TD
    CYCLE[자동매매 사이클] --> STEP1[1. 계좌 정보 동기화]
    STEP1 --> ERROR1{오류 발생?}
    ERROR1 -->|있음| LOG1[오류 로그 + 카운트 증가]
    ERROR1 -->|없음| STEP2[2. 관심종목 동기화]
    
    LOG1 --> STEP2
    STEP2 --> ERROR2{오류 발생?}
    ERROR2 -->|있음| LOG2[오류 로그 + 카운트 증가]
    ERROR2 -->|없음| STEP3[3. 시그널 분석]
    
    LOG2 --> STEP3
    STEP3 --> ERROR3{오류 발생?}
    ERROR3 -->|있음| LOG3[오류 로그 + 카운트 증가]
    ERROR3 -->|없음| STEP4[4. 주문 실행]
    
    LOG3 --> STEP4
    STEP4 --> ERROR4{오류 발생?}
    ERROR4 -->|있음| LOG4[오류 로그 + 카운트 증가]
    ERROR4 -->|없음| STEP5[5. 알림 처리]
    
    LOG4 --> STEP5
    STEP5 --> ERROR5{오류 발생?}
    ERROR5 -->|있음| LOG5[오류 로그 + 카운트 증가]
    ERROR5 -->|없음| STEP6[6. 주문 상태 추적]
    
    LOG5 --> STEP6
    STEP6 --> ERROR6{오류 발생?}
    ERROR6 -->|있음| LOG6[오류 로그 + 카운트 증가]
    ERROR6 -->|없음| COMPLETE[사이클 완료]
    
    LOG6 --> COMPLETE
    COMPLETE --> ERROR_COUNT{총 오류 개수}
    ERROR_COUNT -->|0-2개| NORMAL[정상 대기 3분]
    ERROR_COUNT -->|3개 이상| BACKOFF[5분 대기]
    ERROR_COUNT -->|전체 실패| FAILURE[2분 대기]
    
    NORMAL --> NEXT[다음 사이클]
    BACKOFF --> NEXT
    FAILURE --> NEXT
```

---

## 🎯 **핵심 개선 사항 요약**

1. **실제 API 연동**: `getPendingOrders()` 실제 KIS API 연동 완료
2. **투자 스타일 기반**: 포지션 사이징, RSI 조건, 최대 종목 수 자동 적용
3. **중복 방지**: 시그널 1시간, 알림 30분 중복 방지
4. **안정성**: 단계별 에러 처리, 백오프 전략, 재진입 방지
5. **성능**: DB 인덱스 최적화, 배치 처리
6. **모니터링**: 상세 로깅, 상태 추적, 통계 수집

**🚀 자동매매 시스템이 완전히 개선되어 안정적이고 효율적으로 운영됩니다!**
