# 거래탭 데이터 소스 문제 분석 및 해결방안

## 🔍 현재 문제점

### 잘못된 데이터 소스 사용
**현재 거래탭 로직:**
```dart
// ❌ 잘못된 방식: 실시간 API에서 거래량 조회
final priceData = await RemoteKisService.instance.getCurrentPrice(stockCode, uid: uid);
double volume = _toDouble(priceData['acml_vol']) ?? 0.0; // 실시간 누적 거래량
```

**문제점:**
1. **실시간 API의 `acml_vol`**: 장중 누적 거래량 (실시간 변동)
2. **차트 데이터의 `volume`**: 해당 거래일의 총 거래량 (고정값)
3. **거래탭에서 보여줘야 할 것**: 해당 거래일의 총 거래량 (차트 데이터)

### 올바른 데이터 소스
**Firestore 구조:**
```
/charts/{종목코드}/daily/{날짜}
├── date: "20250926"
├── open: 75000
├── high: 76000
├── low: 74000
├── close: 75500
├── volume: 1234567  ← 이 값이 거래탭에서 표시되어야 함
└── trade_amount: 12345678900
```

## 🔧 해결방안

### 1. 거래탭 데이터 조회 로직 수정

**현재 (잘못된 방식):**
```dart
// 실시간 API에서 거래량 조회
final priceData = await RemoteKisService.instance.getCurrentPrice(stockCode, uid: uid);
double volume = _toDouble(priceData['acml_vol']) ?? 0.0;
```

**수정 후 (올바른 방식):**
```dart
// 1. 실시간 현재가 조회 (가격 정보용)
final priceData = await RemoteKisService.instance.getCurrentPrice(stockCode, uid: uid);

// 2. 차트 데이터에서 최신 거래일 거래량 조회
final chartData = await _getLatestChartData(stockCode);
double volume = _getLatestVolumeFromChart(chartData);
```

### 2. 차트 데이터 조회 함수 추가

```dart
/// 최신 차트 데이터 조회 (Firestore에서)
Future<List<Map<String, dynamic>>> _getLatestChartData(String stockCode) async {
  try {
    final snapshot = await FirebaseFirestore.instance
        .collection('charts')
        .doc(stockCode)
        .collection('daily')
        .orderBy('date_ts', descending: true)
        .limit(1)
        .get();
    
    return snapshot.docs.map((doc) => doc.data()).toList();
  } catch (e) {
    print('❌ 차트 데이터 조회 실패: $e');
    return [];
  }
}

/// 최신 거래일 거래량 조회
double _getLatestVolumeFromChart(List<Map<String, dynamic>> chartData) {
  if (chartData.isEmpty) return 0.0;
  
  final latestData = chartData.first;
  final volume = _toDouble(latestData['volume']) ?? 0.0;
  
  print('📊 최신 거래일 거래량: ${latestData['date']} - $volume');
  return volume;
}
```

### 3. 거래탭 위젯 수정

**현재 (잘못된 방식):**
```dart
double volume = _toDouble(stockData['acml_vol']) ?? 0.0;
```

**수정 후 (올바른 방식):**
```dart
// 거래탭에서는 차트 데이터의 거래량 사용
double volume = await _getVolumeFromChartData(stockCode);
```

### 4. 통합 데이터 조회 서비스

```dart
/// 거래탭용 통합 데이터 조회
Future<Map<String, dynamic>> getTradingScreenData(String stockCode) async {
  try {
    // 1. 실시간 현재가 조회 (가격 정보)
    final priceData = await RemoteKisService.instance.getCurrentPrice(stockCode);
    
    // 2. 차트 데이터에서 최신 거래일 정보 조회
    final chartData = await _getLatestChartData(stockCode);
    final latestChart = chartData.isNotEmpty ? chartData.first : {};
    
    // 3. 통합 데이터 반환
    return {
      // 실시간 가격 정보
      'currentPrice': priceData['currentPrice'],
      'change': priceData['change'],
      'changeRate': priceData['changeRate'],
      'open': priceData['open'],
      'high': priceData['high'],
      'low': priceData['low'],
      
      // 차트 데이터의 거래량 (해당 거래일 총 거래량)
      'volume': latestChart['volume'] ?? 0,
      'tradeAmount': latestChart['trade_amount'] ?? 0,
      'date': latestChart['date'] ?? '',
    };
  } catch (e) {
    print('❌ 거래탭 데이터 조회 실패: $e');
    return {};
  }
}
```

## 🚀 구현 순서

### 1단계: 차트 데이터 조회 함수 추가
- [ ] `_getLatestChartData()` 함수 구현
- [ ] `_getLatestVolumeFromChart()` 함수 구현
- [ ] Firestore 쿼리 최적화

### 2단계: 거래탭 데이터 조회 로직 수정
- [ ] 실시간 현재가와 차트 데이터 분리
- [ ] 거래량은 차트 데이터에서만 조회
- [ ] 통합 데이터 조회 서비스 구현

### 3단계: UI 위젯 수정
- [ ] `StockInfoHeaderWidget` 수정
- [ ] 거래량 표시 로직 개선
- [ ] 에러 처리 및 폴백 로직

### 4단계: 테스트 및 검증
- [ ] 거래탭에서 정확한 거래량 표시 확인
- [ ] 실시간 업데이트 동작 확인
- [ ] 성능 최적화 확인

## 📊 예상 결과

### 수정 전 (현재)
- 거래량 소스: 실시간 API `acml_vol` (장중 누적 거래량)
- 문제점: 실시간으로 변동하는 누적 거래량 표시
- 결과: 25일 거래량이 표시될 수 있음

### 수정 후 (개선)
- 거래량 소스: 차트 데이터 `volume` (해당 거래일 총 거래량)
- 장점: 해당 거래일의 정확한 총 거래량 표시
- 결과: 26일(최신 거래일) 거래량 정확히 표시

## 🔍 데이터 흐름 비교

### 현재 (잘못된 흐름)
```
거래탭 → 실시간 API → acml_vol (누적 거래량) → UI 표시
```

### 수정 후 (올바른 흐름)
```
거래탭 → 실시간 API (가격) + 차트 데이터 (거래량) → UI 표시
       ↓                    ↓
   현재가, 변동률      해당 거래일 총 거래량
```

## ⚠️ 주의사항

1. **데이터 일관성**: 실시간 가격과 차트 거래량의 날짜 일치성 확인
2. **성능 최적화**: Firestore 쿼리 최적화 및 캐싱 전략
3. **에러 처리**: 차트 데이터가 없을 때의 폴백 로직
4. **실시간 업데이트**: 차트 데이터 업데이트 시 UI 반영

## 🎯 핵심 포인트

**거래탭에서 보여줘야 할 거래량:**
- ❌ 실시간 API의 누적 거래량 (`acml_vol`)
- ✅ 차트 데이터의 해당 거래일 총 거래량 (`volume`)

**데이터 소스 분리:**
- 실시간 가격 정보: `getCurrentPrice()` API
- 거래일 거래량 정보: `/charts/{종목코드}/daily` 컬렉션
