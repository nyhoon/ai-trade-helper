# 메모리 부족 문제 해결 순서도

## 🔍 문제 분석
```
스플래시 화면 98% → Performance Benchmarks → SQLite 쿼리 → OutOfMemoryError
```

## 📊 메모리 사용량 분석
- **현재 메모리 사용량**: 190MB (한계: 201MB)
- **할당 실패**: 40MB 할당 시도 → 25MB 여유 공간 부족
- **원인**: 대용량 SQLite 쿼리 결과를 메모리에 한 번에 로드

## 🛠️ 해결 방안

### 1. 스플래시 화면 최적화
```
기존: 모든 관심종목 데이터를 배치로 로드
개선: 필수 데이터만 로드, 나머지는 백그라운드에서 처리
```

### 2. 데이터베이스 쿼리 최적화
```
기존: getActiveStocksChartData() - 모든 활성 종목 데이터 한 번에 로드
개선: 페이지네이션, LIMIT 절 추가, 필요한 컬럼만 SELECT
```

### 3. 메모리 관리 개선
```
- 대용량 데이터 스트리밍 처리
- 불필요한 데이터 즉시 해제
- 캐시 크기 제한
```

### 4. Performance Benchmarks 최적화
```
기존: 실제 대용량 쿼리 실행
개선: 시뮬레이션된 테스트 또는 샘플 데이터 사용
```

## 🚀 구현 순서
1. 스플래시 화면 데이터 로딩 최적화
2. 데이터베이스 쿼리 페이지네이션 적용
3. 메모리 사용량 모니터링 강화
4. Performance Benchmarks 최적화
5. 메모리 누수 방지 코드 추가
