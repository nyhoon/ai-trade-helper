# 거래탭 거래량 표시 문제 해결 순서도

## 🔍 문제 상황
- **거래탭**: 삼성전자 26일 거래량 대신 25일 거래량 표시
- **예상**: 26일(최신 거래일) 거래량이 표시되어야 함
- **실제**: 25일 거래량이 표시됨

## 📊 문제 원인 분석

### 1. 실시간 현재가 조회 로직 문제
**파일**: `lib/features/trading/trading_screen.dart:223-235`

```dart
// 현재가 10초 보장 트리거 (관심/보유/현재 보고있는 종목)
_priceEnsureTimer = Timer.periodic(const Duration(seconds: 10), (timer) async {
  try {
    final uid = FirebaseAuth.instance.currentUser?.uid;
    if (uid == null) return;
    final symbols = await _collectSymbols();
    symbols.add(_selectedStock);
    for (final code in symbols) {
      await UnifiedStockService.instance.getCurrentPrice(code, uid: uid); // 통일된 서비스 사용
    }
  } catch (e) {
    print('⚠️ 10초 현재가 보장 트리거 실패: $e');
  }
});
```

**문제점**: 
- 10초마다 현재가 조회하지만 거래량 데이터가 최신화되지 않을 수 있음
- 캐시 우선 로직으로 인해 오래된 데이터가 표시될 수 있음

### 2. 캐시 데이터 업데이트 로직 문제
**파일**: `lib/core/state/trading_bloc.dart:178-229`

```dart
// 1. 캐시된 데이터 우선 확인
final cachedData = _appDataManager.getCachedStockData(stockCode);
if (cachedData.isNotEmpty) {
  final cachedNorm = _normalizePriceData(stockCode, cachedData);
  final currentNorm = _normalizePriceData(stockCode, currentData);
  final cachedPrice = (cachedNorm['currentPrice'] as num?)?.toDouble() ?? 0.0;
  final currentPrice = (currentNorm['currentPrice'] as num?)?.toDouble() ?? 0.0;
  
  // 캐시된 가격이 유효하고 현재 가격과 다르면 업데이트
  if (cachedPrice > 0 && (cachedPrice - currentPrice).abs() > 0.01) {
    print('📊 TradingBloc: 캐시된 데이터로 업데이트 - $stockCode ($currentPrice → $cachedPrice)');
    return cachedNorm;
  }
}
```

**문제점**:
- 가격만 비교하고 거래량은 비교하지 않음
- 오래된 거래량 데이터가 계속 표시될 수 있음

### 3. 거래량 데이터 소스 문제
**파일**: `lib/features/trading/widgets/stock_info_header_widget.dart:36-57`

```dart
double volume = _toDouble(stockData['acml_vol']) ?? 0.0;
if (volume <= 0) {
  final cachedData = AppDataManager.instance.getCachedStockData(stockCode);
  final cachedVol = _toDouble(cachedData['acml_vol'])
      + _toDouble(cachedData['volume']);
  if (cachedVol > 0) {
    volume = cachedVol;
    print('🔍 [StockInfoHeader] 거래량 캐시 폴백 사용: $volume');
  } else {
    try {
      final cachedChart = AppDataManager.instance.getCachedChartData(stockCode);
      if (cachedChart.isNotEmpty) {
        final last = cachedChart.last;
        final chartVol = _toDouble(last['volume']);
        if (chartVol > 0) {
          volume = chartVol;
          print('🔍 [StockInfoHeader] 거래량 차트 폴백 사용: $volume');
        }
      }
    } catch (_) {}
  }
}
```

**문제점**:
- 차트 데이터의 마지막 거래일이 25일일 수 있음
- 실시간 거래량 데이터가 제대로 반영되지 않음

## 🔧 해결 방안

### 1. 실시간 현재가 조회 로직 개선
```dart
// 개선된 현재가 조회 로직
_priceEnsureTimer = Timer.periodic(const Duration(seconds: 10), (timer) async {
  try {
    final uid = FirebaseAuth.instance.currentUser?.uid;
    if (uid == null) return;
    final symbols = await _collectSymbols();
    symbols.add(_selectedStock);
    
    for (final code in symbols) {
      // 강제로 최신 데이터 조회 (캐시 무시)
      await _forceRefreshCurrentPrice(code, uid);
    }
  } catch (e) {
    print('⚠️ 10초 현재가 보장 트리거 실패: $e');
  }
});

// 강제 최신 데이터 조회
Future<void> _forceRefreshCurrentPrice(String stockCode, String uid) async {
  try {
    // 1. API에서 최신 데이터 조회
    final priceData = await RemoteKisService.instance.getCurrentPrice(stockCode, uid: uid);
    
    if (priceData != null && priceData.isNotEmpty) {
      // 2. 캐시 강제 업데이트
      AppDataManager.instance.updateCurrentPrice(stockCode, priceData);
      
      // 3. UI 상태 업데이트
      if (mounted) {
        setState(() {
          // UI 강제 업데이트
        });
      }
      
      print('📊 강제 현재가 업데이트 완료: $stockCode');
    }
  } catch (e) {
    print('❌ 강제 현재가 업데이트 실패: $stockCode - $e');
  }
}
```

### 2. 캐시 데이터 업데이트 로직 개선
```dart
// 개선된 캐시 데이터 확인 로직
Future<Map<String, dynamic>> _loadStockDataSilent(String stockCode, Map<String, dynamic> currentData) async {
  try {
    print('📊 TradingBloc: 사일런트 현재가 데이터 로드 - $stockCode');
    
    // 1. 캐시된 데이터 확인 (거래량도 함께 확인)
    final cachedData = _appDataManager.getCachedStockData(stockCode);
    if (cachedData.isNotEmpty) {
      final cachedNorm = _normalizePriceData(stockCode, cachedData);
      final currentNorm = _normalizePriceData(stockCode, currentData);
      
      final cachedPrice = (cachedNorm['currentPrice'] as num?)?.toDouble() ?? 0.0;
      final currentPrice = (currentNorm['currentPrice'] as num?)?.toDouble() ?? 0.0;
      final cachedVolume = (cachedNorm['volume'] as num?)?.toDouble() ?? 0.0;
      final currentVolume = (currentNorm['volume'] as num?)?.toDouble() ?? 0.0;
      
      // 가격 또는 거래량이 변경된 경우 업데이트
      final priceChanged = cachedPrice > 0 && (cachedPrice - currentPrice).abs() > 0.01;
      final volumeChanged = cachedVolume > 0 && (cachedVolume - currentVolume).abs() > 0.01;
      
      if (priceChanged || volumeChanged) {
        print('📊 TradingBloc: 캐시된 데이터로 업데이트 - $stockCode (가격: $priceChanged, 거래량: $volumeChanged)');
        return cachedNorm;
      }
    }
    
    // 2. 서버에서 최신 데이터 조회
    final uid = FirebaseAuth.instance.currentUser?.uid ?? 'debug-user';
    await RemoteKisService.instance.ensureChartAndAnalyze(uid: uid, symbol: stockCode);
    final priceData = await RemoteKisService.instance.getCurrentPrice(stockCode, uid: uid);
    
    if (priceData != null && priceData.isNotEmpty) {
      final normalized = _normalizePriceData(stockCode, priceData);
      
      final newPrice = (normalized['currentPrice'] as num?)?.toDouble() ?? 0.0;
      final newVolume = (normalized['volume'] as num?)?.toDouble() ?? 0.0;
      final currentNorm = _normalizePriceData(stockCode, currentData);
      final currentPrice = (currentNorm['currentPrice'] as num?)?.toDouble() ?? 0.0;
      final currentVolume = (currentNorm['volume'] as num?)?.toDouble() ?? 0.0;
      
      // 가격 또는 거래량이 변경된 경우에만 업데이트
      final priceChanged = newPrice > 0 && (newPrice - currentPrice).abs() > 0.01;
      final volumeChanged = newVolume > 0 && (newVolume - currentVolume).abs() > 0.01;
      
      if (priceChanged || volumeChanged) {
        print('📊 TradingBloc: 서버 현재가로 업데이트 - $stockCode (가격: $priceChanged, 거래량: $volumeChanged)');
        
        // 캐시에 저장
        _appDataManager.updateCurrentPrice(stockCode, normalized);
        
        return normalized;
      } else {
        print('📊 TradingBloc: 가격/거래량 변경 없음 - $stockCode');
        return currentNorm;
      }
    } else {
      print('⚠️ TradingBloc: 서버 현재가 없음 - $stockCode');
      return _normalizePriceData(stockCode, currentData);
    }
  } catch (e) {
    print('❌ TradingBloc: 사일런트 현재가 로드 실패 - $stockCode: $e');
    return _normalizePriceData(stockCode, currentData);
  }
}
```

### 3. 거래량 데이터 소스 개선
```dart
// 개선된 거래량 데이터 조회 로직
double _getCurrentVolume(String stockCode, Map<String, dynamic> stockData) {
  // 1. 실시간 데이터 우선
  double volume = _toDouble(stockData['acml_vol']) ?? 0.0;
  
  if (volume <= 0) {
    // 2. 캐시된 실시간 데이터 확인
    final cachedData = AppDataManager.instance.getCachedStockData(stockCode);
    final cachedVol = _toDouble(cachedData['acml_vol']) + _toDouble(cachedData['volume']);
    
    if (cachedVol > 0) {
      volume = cachedVol;
      print('🔍 [StockInfoHeader] 거래량 캐시 사용: $volume');
    } else {
      // 3. 차트 데이터에서 최신 거래량 확인
      try {
        final cachedChart = AppDataManager.instance.getCachedChartData(stockCode);
        if (cachedChart.isNotEmpty) {
          // 최신 거래일 데이터 확인
          final today = DateTime.now();
          final todayStr = '${today.year}${today.month.toString().padLeft(2, '0')}${today.day.toString().padLeft(2, '0')}';
          
          // 오늘 날짜 데이터 우선 확인
          for (final chartData in cachedChart.reversed) {
            final chartDate = chartData['date']?.toString() ?? '';
            if (chartDate == todayStr) {
              final chartVol = _toDouble(chartData['volume']);
              if (chartVol > 0) {
                volume = chartVol;
                print('🔍 [StockInfoHeader] 오늘 거래량 사용: $volume (날짜: $chartDate)');
                break;
              }
            }
          }
          
          // 오늘 데이터가 없으면 최신 데이터 사용
          if (volume <= 0) {
            final last = cachedChart.last;
            final chartVol = _toDouble(last['volume']);
            if (chartVol > 0) {
              volume = chartVol;
              print('🔍 [StockInfoHeader] 최신 거래량 사용: $volume');
            }
          }
        }
      } catch (e) {
        print('❌ [StockInfoHeader] 거래량 조회 실패: $e');
      }
    }
  }
  
  return volume;
}
```

## 🚀 구현 순서

### 1단계: 실시간 현재가 조회 로직 개선
- [ ] 강제 최신 데이터 조회 함수 추가
- [ ] 거래량 데이터도 함께 확인하도록 로직 개선
- [ ] 캐시 무시 옵션 추가

### 2단계: 캐시 데이터 업데이트 로직 개선
- [ ] 거래량 변경 감지 로직 추가
- [ ] 가격과 거래량 모두 확인하도록 개선
- [ ] 강제 업데이트 옵션 추가

### 3단계: 거래량 데이터 소스 개선
- [ ] 오늘 날짜 데이터 우선 확인 로직
- [ ] 실시간 거래량 데이터 우선 사용
- [ ] 폴백 로직 개선

### 4단계: 테스트 및 검증
- [ ] 거래탭에서 삼성전자 거래량 확인
- [ ] 실시간 업데이트 동작 확인
- [ ] 최신 거래일 데이터 표시 확인

## 📋 예상 결과

### 수정 전
- 거래량: 25일 거래량 표시
- 업데이트: 가격만 확인
- 데이터 소스: 오래된 캐시 데이터

### 수정 후
- 거래량: 26일(최신 거래일) 거래량 표시
- 업데이트: 가격 + 거래량 모두 확인
- 데이터 소스: 실시간 API 데이터 우선

## 🔍 모니터링 포인트

1. **실시간 데이터 업데이트**: 10초마다 정상적으로 실행되는지 확인
2. **거래량 데이터 정확성**: 최신 거래일 거래량이 표시되는지 확인
3. **캐시 업데이트**: 새로운 데이터가 캐시에 정상적으로 저장되는지 확인
4. **UI 업데이트**: 거래탭에서 실시간으로 거래량이 업데이트되는지 확인

## ⚠️ 주의사항

1. **API 호출 제한**: KIS API 호출 제한을 고려하여 적절한 딜레이 설정
2. **캐시 일관성**: 실시간 데이터와 캐시 데이터 간의 일관성 유지
3. **성능 최적화**: 불필요한 API 호출 방지
4. **에러 처리**: API 호출 실패 시 적절한 폴백 로직
